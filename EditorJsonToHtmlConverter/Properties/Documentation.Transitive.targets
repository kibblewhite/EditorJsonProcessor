<Project>

	<!-- // Packed as buildTransitive\EditorJsonToHtmlConverter.targets. Flows the documentation copy past a direct -->
	<!-- // reference, so an application that consumes this package only through a library project still receives the -->
	<!-- // file beside its executable. Mirrors how this package's SDK-generated *.props already chain back to build\. -->
	<Import Project="..\build\EditorJsonToHtmlConverter.targets" />

</Project>
